source 'http://rubygems.org'

# gem 'rails', :git => 'git://github.com/rails/rails.git'
gem 'rails', :path => File.join(File.dirname(__FILE__), '/vendor/gems/rails')
# gem 'arel',  :git => 'git://github.com/rails/arel.git'
gem 'arel', :path => File.join(File.dirname(__FILE__), '/vendor/gems/arel')

gem 'pg'

# gem 'formtastic', :git => 'git://github.com/justinfrench/formtastic.git', :branch => 'rails3'
gem 'formtastic', :path => File.join(File.dirname(__FILE__), '/vendor/gems/formtastic')

# gem 'devise', :git => 'git://github.com/plataformatec/devise.git'
gem 'devise', :path => File.join(File.dirname(__FILE__), '/vendor/gems/devise')

# for devise:views
gem 'hpricot' 
gem 'ruby_parser'

gem 'haml'
gem 'compass'
gem 'fancy-buttons'
gem 'validation_reflection'
gem 'heroku'

group :test do
  # gem 'capybara', :git => 'git://github.com/jnicklas/capybara.git'
  gem 'capybara', :path => File.join(File.dirname(__FILE__), '/vendor/gems/capybara')
  gem 'launchy'
  # gem 'shoulda', :git => 'git://github.com/thoughtbot/shoulda.git'
  gem 'shoulda', :path => File.join(File.dirname(__FILE__), '/vendor/gems/shoulda')
  # gem 'machinist', :git => 'git://github.com/notahat/machinist.git'
  gem 'machinist', :path => File.join(File.dirname(__FILE__), '/vendor/gems/machinist')
  gem 'mocha'
  gem 'database_cleaner'
end

# Use unicorn as the web server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'ruby-debug'

# Bundle the extra gems:
# gem 'bj'
# gem 'nokogiri', '1.4.1'
# gem 'sqlite3-ruby', :require => 'sqlite3'
# gem 'aws-s3', :require => 'aws/s3'

# Bundle gems for the local environment. Make sure to
# put test-only gems in this group so their generators
# and rake tasks are available in development mode:
# group :development, :test do
#   gem 'webrat'
# end
